//Router.go(Router.current().request.url + '/modal');
//console.log("Hello gsdgdfgdf");
//console.log(Router.current().req.url);
//var currentState = history.state;
//console.log(currentState);

if (Meteor.isClient) {
  
};
